The **GetCommandItem** method retrieves a specific command item by its ID. This enables users to pin nested commands (not just top-level ones) to the Dock.

## Parameters

*id* **String**

The ID of the command item to retrieve.

## Returns

An [`ICommandItem`] that contains the command item associated with the specified ID.
